services:
  api:
    build: ./api
    expose:
      - "3000"
    networks:
      - internal

  frontend:
    build: ./frontend
    expose:
      - "80"
    depends_on:
      - api
    networks:
      - internal
      - coolify

networks:
  internal:
  coolify:
    external: true
